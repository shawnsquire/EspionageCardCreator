# EspionageCardCreator
Automatically generate cards from CSV, and turn them into a sprite sheet, and upload to Imgur.

## Getting started
    $ sudo pip install pillow
    $ sudo pip install imgurpython
